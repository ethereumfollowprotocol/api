{
  "name": "efp-public-api",
  "version": "0.0.0",
  "description": "Ethereum Follow Protocol Public API",
  "type": "module",
  "scripts": {
    "dev": "bunx wrangler dev --experimental-json-config --config=wrangler.json --latest=true",
    "dev:remote": "bun dev --remote",
    "build": "bunx wrangler deploy --dry-run --experimental-json-config --config=wrangler.json --outdir=dist --minify=true --latest=true --compatibility-flag=node_compat --compatibility-date=2023-06-30",
    "database:generate-types": "kysely-codegen --dialect='postgres' --type-only-imports --log-level='error' --out-file='./src/types/generated/index.ts' && bun format",
    "lint": "biome check --apply .",
    "format": "biome format . --write",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "clean": "rm -rf dist .wrangler node_modules",
    "update-dependencies": "bun ./scripts/update-dependencies.ts"
  },
  "dependencies": {
    "consola": "^3.2.3",
    "hono": "^3.11.6",
    "kysely": "^0.26.3",
    "kysely-postgres-js": "^2.0.0",
    "postgres": "^3.4.3",
    "pretty-cache-header": "^1.0.0",
    "viem": "^2.0.0-beta.15"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.4.1",
    "@cloudflare/workers-types": "^4.20231121.0",
    "@total-typescript/ts-reset": "^0.5.1",
    "@types/node": "^20.10.4",
    "bun": "^1.0.17",
    "bun-types": "^1.0.17",
    "kysely-codegen": "^0.11.0",
    "pg": "^8.11.3",
    "typescript": "^5.3.3",
    "wrangler": "^3.20.0"
  },
  "sideEffects": false,
  "repository": "github:ethereumfollowprotocol/api",
  "homepage": "github:ethereumfollowprotocol/indexer/README.md"
}
